val newVersionCode = if (json.has("versionCode")) {
    json.getInt("versionCode")
} else {
    json.optInt("latest_version_code", 0)
}

val webPageUrl = json.optString("download_url", json.optString("downloadUrl", "https://t.me/XMods_XM"))
val description = json.optString("release_notes", json.optString("description", "A new version is available!"))

